<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="su.android.R" />

        <variable
            name="item"
            type="su.android.ui.deny.DenyListRvItem" />

        <variable
            name="viewModel"
            type="su.android.ui.deny.DenyListViewModel" />

    </data>

    <com.google.android.material.card.MaterialCardView
        style="@style/WidgetFoundation.Card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="false"
        tools:layout_gravity="center"
        tools:layout_marginBottom="@dimen/l1">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/HideExpand"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="?selectableItemBackground"
                android:clickable="true"
                android:focusable="true"
                android:nextFocusRight="@id/HideExpandIcon"
                android:onClick="@{item::toggleExpand}">

                <ImageView
                    android:id="@+id/HideIcon"
                    style="@style/WidgetFoundation.Image"
                    android:layout_margin="@dimen/l1"
                    android:src="@{item.info.iconImage}"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintVertical_bias="0"
                    tools:src="@drawable/LauncherIcon" />

                <TextView
                    android:id="@+id/HideName"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/l1"
                    android:ellipsize="middle"
                    android:singleLine="true"
                    android:text="@{item.info.label}"
                    android:textAppearance="@style/AppearanceFoundation.Body"
                    android:textStyle="bold"
                    app:layout_constraintBottom_toTopOf="@+id/HidePackage"
                    app:layout_constraintEnd_toStartOf="@+id/HideExpandIcon"
                    app:layout_constraintStart_toEndOf="@+id/HideIcon"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintVertical_chainStyle="packed"
                    tools:text="@string/supersu" />

                <TextView
                    android:id="@+id/HidePackage"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:text="@{item.info.packageName}"
                    android:textAppearance="@style/AppearanceFoundation.Caption.Variant"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="@+id/HideName"
                    app:layout_constraintStart_toStartOf="@+id/HideName"
                    app:layout_constraintTop_toBottomOf="@+id/HideName"
                    tools:text="su.android" />

                <com.topjohnwu.widget.IndeterminateCheckBox
                    android:id="@+id/HideExpandIcon"
                    state="@={item.state}"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="@dimen/l1"
                    android:minWidth="0dp"
                    android:minHeight="0dp"
                    android:nextFocusLeft="@id/HideExpand"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

            </androidx.constraintlayout.widget.ConstraintLayout>

            <androidx.recyclerview.widget.RecyclerView
                goneUnless="@{item.isExpanded}"
                app:items="@{item.processes}"
                app:extraBindings="@{viewModel.extraBindings}"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="?colorSurfaceVariant"
                android:orientation="vertical"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                tools:itemCount="2"
                tools:listitem="@layout/ItemHideProcess" />

        </LinearLayout>

        <ProgressBar
            style="@style/WidgetFoundation.ProgressBar"
            gone="@{item.checkedPercent == 0}"
            android:layout_width="match_parent"
            android:layout_gravity="top"
            android:progress="@{item.checkedPercent}" />

    </com.google.android.material.card.MaterialCardView>

</layout>
