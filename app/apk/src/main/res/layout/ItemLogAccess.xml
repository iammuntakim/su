<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="su.android.R" />

        <variable
            name="item"
            type="su.android.ui.log.LogEntryItem" />

        <variable
            name="viewModel"
            type="su.android.ui.log.LogViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?listPreferredItemHeightSmall"
        tools:layout_gravity="center">

        <include
            android:id="@+id/LogTrackContainer"
            bullet="@{item.log.action >= 2 ? R.drawable.ic_check : R.drawable.ic_close}"
            isBottom="@{item.isBottom}"
            isSelected="@{item.log.action != 2}"
            isTop="@{item.isTop}"
            layout="@layout/ItemLogTrack"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/l1"
            android:paddingTop="@dimen/l_50"
            android:paddingBottom="@dimen/l_50"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/LogTrackContainer"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/LogAppName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{item.log.appName}"
                android:textAppearance="@style/AppearanceFoundation.Body"
                android:textStyle="bold"
                app:layout_constraintBottom_toTopOf="@+id/LogInfo"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="@string/supersu" />

            <TextView
                android:id="@+id/LogInfo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{item.info}"
                android:textAppearance="@style/AppearanceFoundation.Caption.Variant"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/LogAppName"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
